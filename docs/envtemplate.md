### El .env actual utiliza lo siguiente

- DB_HOST=(para uso local, usar localhost)
- DB_PORT=(puerto al que se conecta, por defecto 5432)
- DB_NAME=(nombre usado para base de datos)
- DB_USER=(nombre de usuario PostgreSQL)
- DB_PASSWORD=(contraseña de base de datos PostgreSQL)
- OWM_API_KEY=(api key para Open Weather Map)
