package michaelsoftbinbows.model;

/**
 * Anotación personalizada para la validación de que un campo no esté vacío (NotBlank). (Usado para
 * validación de beans, por ejemplo).
 */
public @interface NotBlank {}
